{
  "name": "static-server",
  "version": "1.0.0",
  "description": "HTTP server for static files",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Fabien Hippolyte",
  "license": "ISC",
  "dependencies" :
  { "node-static" : "0.7.7"
  }
}
